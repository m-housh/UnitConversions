import XCTest
@testable import UnitConversions

struct ExpectedContainer<T> {
    let type: T
    let result: Double
}


class UnitConversionsTests: XCTestCase {
    // This should possible have default implementation and we can subclass
    // for all of our tests.
}
